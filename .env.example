# Optional API keys for future connectors. The default live refresh does not require them.
# Do not commit a real .env file.
FRED_API_KEY=
EIA_API_KEY=

# Optional local overrides.
REQUEST_TIMEOUT_SECONDS=20
CYCLE_RADAR_ROOT=
